[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: serve index.html for all routes (e.g. /login, /dashboard, /positions, etc.)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
